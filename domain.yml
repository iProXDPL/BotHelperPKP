version: "3.1"

intents:
  - greet
  - goodbye
  - ask_weather

responses:
  utter_greet:
    - text: "Witaj! Jak mogę pomóc?"

  utter_goodbye:
    - text: "Do zobaczenia!"

actions:
  - action_show_weather

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
